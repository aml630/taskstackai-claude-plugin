---
description: Show blocked issues
---

Show all issues that are currently blocked by dependencies.

Run:
```
${CLAUDE_PLUGIN_DIR}/bin/taskstackai blocked
```

Present the results clearly, showing each blocked issue and what is blocking it. Suggest the user resolve blockers or use `/dep` to manage dependencies.
