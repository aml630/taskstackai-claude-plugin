---
description: Remove TaskStackAi from this project
---

Remove TaskStackAi configuration from the current project.

1. Run:
```
${CLAUDE_PLUGIN_DIR}/bin/taskstackai remove
```
2. Confirm what was removed (config, AGENTS.md, .gitignore entry).
3. Let the user know they can re-initialize anytime with `/init --key <key>`.
