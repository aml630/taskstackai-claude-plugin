---
description: Show taskstackai CLI version
---

Display the current version of the taskstackai CLI tool.

Run:
```
${CLAUDE_PLUGIN_DIR}/bin/taskstackai version
```

Show the version number to the user.
